<div class="container">
  <div class="login-container" style="max-width: 400px; margin: 40px auto; padding: 30px; box-shadow: 0 0 10px rgba(0,0,0,0.1); border-radius: 8px;">
    <h2 style="text-align: center; margin-bottom: 30px; color: #333;">ログイン</h2>

    <%= form_with(url: login_path, local: true) do |f| %>
      <% if flash[:alert] %>
        <div class="alert alert-danger">
          <ul style="color: #dc3545; list-style-type: none; margin: 0; padding: 0;">
            <% flash[:alert].split(', ').each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-group">
        <%= f.label :login, 'ログインID', style: "display: block; margin-bottom: 8px; color: #555;" %>
        <%= f.text_field :login, class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= f.label :password, 'パスワード', style: "display: block; margin-bottom: 8px; color: #555;" %>
        <%= f.password_field :password, class: 'form-control' %>
      </div>

      <div class="form-group" style="text-align: center;">
        <%= f.submit 'ログイン', class: 'btn btn-primary', style: "width: 100%;" %>
      </div>
    <% end %>
  </div>
</div> 